<?php

namespace Rancher\Filter;

use Rancher\AbstractFilter;

class {{ name }} extends AbstractFilter
{
{% for key, filter in filters %}
    /**
     * Gets {{ key }}
     * @return string
     */
    public function get{{ key|ucfirst }}()
    {
        return $this->container['{{ key }}'];
    }

    /**
     * Sets {{ key }}
     * @param string ${{ key }}
     * @param string $option
     * @return $this
     */
    public function set{{ key|ucfirst }}(${{ key }}, $option = '')
    {
        if(!empty($option) && in_array($option, $this->allowedOptions))
        {
            $option = '_'.$option;
        }

        $this->container['{{ key }}'.$option] = ${{ key }};

        return $this;
    }
{% if not loop.last %}


{% endif %}
{% endfor %}
}